{# src/Okeanos/CoreBundle/Resources/views/Default/admin.html.twig #}
{% extends 'OkeanosCoreBundle::layout.html.twig' %}
{% block title %}Admin | {{ parent () }}{% endblock %}
{% block activePageAdmin %} active{% endblock %}
{% block container %}
    <div class="row justify-content-center mt-3">
        <div class="col-sm-6">
            <p>
                Select the category :
            </p>
            <p>
                <a href="{{ path('okeanos_core_users') }}" class="btn btn-primary">Users</a>
                <a href="{{ path('okeanos_core_actionsAdd') }}" class="btn btn-primary">Add actions</a>
                <a href="{{ path('okeanos_core_newsAdd') }}" class="btn btn-primary">Add news</a>
            </p>
        </div>
    </div>
{% endblock %}